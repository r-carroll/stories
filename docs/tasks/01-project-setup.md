# Task: Foundation & Project Setup

**Priority**: High
**Effort**: Small
**Dependencies**: None

## Objectives
Initialize the core application structure so that feature development can proceed in parallel.

## Sub-Tasks
- [x] **Dependencies**: Verify `watermelondb`, `lucide-react-native`, `expo-router`, `react-native-reanimated` are installed and configured.
- [x] **Theme Context**: Implement `ThemeContext` or `Colors.ts` using the **Modern Cozy** palette (Charcoal `#0F172A`, Ember Orange `#F97316`).
- [x] **Navigation Shell**: Create the `app/_layout.tsx` with a basic background container that applies the global dark theme.
- [x] **Fonts**: Configure `Young Serif` (headings) and `Inter` (body) in `expo-font`.

## Acceptance Criteria
- App runs without errors.
- Global background color is `#0F172A`.
- Basic routing works.
